<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Complaints</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #181818; /* Dark background */
            color: #fff; /* Light text */
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #333; /* Darker background */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.1); /* Soft shadow */
        }

        h1 {
            color: #4CAF50; /* Green title */
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin-bottom: 20px;
            border-bottom: 1px solid #555; /* Dark border */
            padding: 10px 0;
        }

        li:last-child {
            border-bottom: none;
        }

        strong {
            color: #4CAF50; /* Green text */
        }

        p {
            color: #888; /* Lighter text */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Your Complaints</h1>
        
        <% if (complaints.length > 0) { %>
            <ul>
                <% complaints.forEach(i => { %>
                    <li>
                        <strong>Complaint Type:</strong> <%= i.complaintType %><br>
                        <strong>Reference ID:</strong> <%= i.referenceID %><br>
                        <strong>Description:</strong> <%= i.complaintMessage %><br>
                        <strong>Status:</strong> <%= i.status %><br>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No complaints found.</p>
        <% } %>
    </div>
</body>
</html>
